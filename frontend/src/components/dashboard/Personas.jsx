import Preinscripcion from './Preinscripcion';

// Simply re-export the Preinscripcion component to show it directly
const Personas = () => {
  return <Preinscripcion />;
};

export default Personas;
