import Acreditacion from './Acreditacion';

// Re-export the Acreditacion component directly (it's already a functional view)
const AccreditationDirect = () => {
    return <Acreditacion />;
};

export default AccreditationDirect;
