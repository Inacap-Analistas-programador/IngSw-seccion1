# SGICS - Configuración de Variables de Entorno
# IMPORTANTE: Nunca subir el archivo .env real a Git
# Este archivo (.env.example) es solo una plantilla

# =============================================================================
# CONFIGURACIÓN BÁSICA DE DJANGO
# =============================================================================

# Clave secreta de Django (generar una nueva para producción)
SECRET_KEY=django-insecure-CAMBIAR-EN-PRODUCCION-usar-django-admin-startproject

# Modo de depuración (False en producción)
DEBUG=True

# Hosts permitidos (separados por comas)
ALLOWED_HOSTS=localhost,127.0.0.1,*.tu-dominio.com

# =============================================================================
# CONFIGURACIÓN DE BASE DE DATOS
# =============================================================================

# Tipo de base de datos: sqlite, mysql, postgresql
DATABASE_ENGINE=sqlite

# Para MySQL/PostgreSQL
DATABASE_NAME=sgics_db
DATABASE_USER=sgics_user
DATABASE_PASSWORD=password_seguro_aqui
DATABASE_HOST=localhost
DATABASE_PORT=3306

# =============================================================================
# CONFIGURACIÓN DE HOSTING (CONFIDENCIAL - NO SUBIR A GIT)
# =============================================================================

# Información del servidor (obtenida de cPanel)
# Servidor: altar39, IP: 170.10.163.116
# Apache: 2.4.65, MySQL: 8.0.43, Sistema: Linux x86_64

# URL del panel de control (CAMBIAR POR TU DOMINIO)
HOSTING_PANEL_URL=cpanel.tu-dominio.com

# Credenciales del hosting (MANTENER SECRETO)
HOSTING_USER=tu_usuario_cpanel
HOSTING_PASSWORD=tu_password_cpanel_seguro
HOSTING_SERVER_NAME=altar39

# Configuración de base de datos MySQL 8.0.43
DATABASE_ENGINE=mysql
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_NAME=sgics_production
DATABASE_USER=sgics_db_user
DATABASE_PASSWORD=password_mysql_seguro

# Configuración FTP/SFTP para deployment
FTP_HOST=tu-dominio.com
FTP_USER=tu_usuario_ftp
FTP_PASSWORD=tu_password_ftp_seguro

# Configuración de Apache 2.4.65
APACHE_VERSION=2.4.65
PERL_PATH=/usr/bin/perl
SENDMAIL_PATH=/usr/sbin/sendmail

# =============================================================================
# CONFIGURACIÓN DE CORREO ELECTRÓNICO
# =============================================================================

# Configuración SMTP
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu_email@gmail.com
EMAIL_HOST_PASSWORD=tu_password_de_aplicacion

# =============================================================================
# CONFIGURACIÓN DE SERVICIOS EXTERNOS
# =============================================================================

# Token de Sonar para análisis de código
SONAR_TOKEN=tu_token_sonar_aqui

# Configuración de Redis (si se usa)
REDIS_URL=redis://localhost:6379/0

# Configuración de APIs de pago (Webpay, etc.)
WEBPAY_ENVIRONMENT=development
WEBPAY_COMMERCE_CODE=tu_codigo_comercio
WEBPAY_API_KEY=tu_api_key_webpay

# =============================================================================
# CONFIGURACIÓN DE LOGGING Y MONITOREO
# =============================================================================

# Nivel de logging: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Sentry DSN para monitoreo de errores (opcional)
SENTRY_DSN=https://tu_sentry_dsn_aqui@sentry.io/proyecto

# =============================================================================
# INSTRUCCIONES DE USO
# =============================================================================

# 1. Copiar este archivo como .env:
#    cp .env.example .env
#
# 2. Editar el archivo .env con tus valores reales
#
# 3. Nunca subir el archivo .env a Git:
#    echo ".env" >> .gitignore
#
# 4. Para producción, usar variables de entorno del sistema:
#    export SECRET_KEY="tu_clave_secreta"
#    export DATABASE_PASSWORD="password_real"

# =============================================================================
# NOTAS DE SEGURIDAD
# =============================================================================

# - Cambiar SECRET_KEY en producción
# - Usar contraseñas fuertes para la base de datos
# - Habilitar HTTPS en producción (SSL/TLS)
# - Configurar firewall para restringir acceso a puertos
# - Hacer backups regulares de la base de datos
# - Mantener Django y dependencias actualizadas